{{#extend "default"}}
  {{#content "main"}}
  <section id='main'>
    <header class='page-title'>
        <h1>Dan's Blog</h1>
        <p class='page-desc'>{{description}}</p>
    </header>

    <hr>

    <ul>
        {{#each collections.blog}}
            <li>
                <strong><a href='/{{this.path}}'>{{this.title}}</a></strong> - <span class='date'>{{this.date}}</span>
                
            </li>
        {{/each}}
    </ul>

    <hr>
  </section>
  {{/content}}
{{/extend}}